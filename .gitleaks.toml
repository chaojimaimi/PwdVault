title = "PwdVault Gitleaks configuration"

[extend]
useDefault = true

[[rules]]
id = "generic-api-key"

[[rules.allowlists]]
description = "Chrome manifest key is a public RSA key used to derive a stable extension ID"
condition = "AND"
paths = ['''^extensions/chrome/manifest\.json$''']
regexTarget = "line"
regexes = ['''^\s*"key":\s*"[A-Za-z0-9+/=]+"\s*,?\s*$''']
